# vim
.sw[a-p]
.*.sw[a-p]
Session.vim
.netrwhist
spell.en.add

# never add .env, may contain secrets or local-only config
.env
# ensure chezmoi  dot_env file doesn't accidentally get added to a repo
dot_env

# nonsense gtk file
.config/gtk-3.0/bookmarks

node_modules
