#!/bin/sh
# Kinda like .xprofile
# noting sessions kicking around after dwl quits. Brute force:
pkill pipewire
pkill dbus-launch

# both started here:
. ~/.wlrprofile
# launch!
/usr/bin/dwl -s /usr/bin/somebar

# noting sessions kicking around after dwl quits. Brute force:
pkill pipewire
pkill dbus-launch
