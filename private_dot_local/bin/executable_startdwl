#!/bin/sh
# Kinda like .xprofile but with configuration for wlroots compositor like dwl
# . ~/.wlrprofile
eval $(gnome-keyring-daemon --start)
export SSH_AUTH_SOCK
exec /usr/bin/dwl
# exec /usr/bin/dwl -s /usr/bin/somebar
