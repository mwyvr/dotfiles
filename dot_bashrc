# .bashrc for Void Linux (and others)
# See notes in ~/.bash_profile.
#
# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# source alias
test -s ~/.alias && . ~/.alias || true

# prompt
PS1='[\u@\h \W]\$ '
