# .bashrc for Void Linux (and others)
#
# PATH and env variables should be defined in ~/.profile and system profile
#
# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# source alias
test -s ~/.alias && . ~/.alias || true

PS1='[\u@\h \W]\$ '
